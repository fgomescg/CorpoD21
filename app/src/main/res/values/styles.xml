<resources>

    <!-- Base application theme. -->
    <style name="FrameworkRoot.Theme" parent="Theme.AppCompat.Light" />
    <style name="FrameworkRoot.Widget" parent="Theme.AppCompat" />

    <!-- Immediate parent theme to specify base values for custom attributes -->
    <style name="Theme" parent="FrameworkRoot.Theme">
        <!-- Only needed for pre-L -->
    </style>

    <style name="AppTheme" parent="Theme">
        <!-- Customize your theme here. -->
        <!--<item name="android:actionBarStyle">@style/ActionBar</item>-->
        <item name="actionBarIconColor">#fff</item>
        <!--<item name="actionBarInsetStart">@dimen/keyline_2</item>-->
        <item name="homeAsUpIndicator">@mipmap/ic_up21</item>

        <!--<item name="popupItemBackground">?android:selectableItemBackground</item>-->

        <item name="colorPrimary">@color/md_red_500</item>
        <item name="colorPrimaryDark">@color/md_red_900</item>
        <item name="colorAccent">@color/theme_accent_1</item>

        <item name="android:textColorLink">@color/theme_accent_1</item>

        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>

        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:homeAsUpIndicator">@mipmap/ic_up21</item>

        <item name="android:editTextStyle">@style/SingleLineEdit</item>

    </style>

    <style name="AppThemeWindowNoTitle" parent="android:Theme">
        <item name="android:windowNoTitle">true</item>

    </style>

    <style name="subheader"/>

    <style name="HeaderBar">
        <item name="android:background">?colorPrimary</item>
    </style>

    <style name="ActionBarThemeOverlay" parent="">
        <item name="android:textColorPrimary">#fff</item>
        <item name="colorControlNormal">?actionBarIconColor</item>
        <item name="colorControlHighlight">#3fff</item>
    </style>

    <style name="ActionBarPopupThemeOverlay" parent="ThemeOverlay.AppCompat.Light" >
        <item name="android:background">@android:color/white</item>
        <item name="android:textColor">#000</item>
    </style>

    <style name="ActionBar.TitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">#fff</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="SingleLineEdit" parent="@android:style/Widget.EditText">
        <item name="android:singleLine">true</item>
        <item name="android:lines">1</item>
        <item name="android:maxLines">1</item>
        <item name="android:background">@drawable/edittext_rounded</item>
        <item name="android:textColorHint">@color/button_material_light</item>
    </style>

    <style name="WheelDemoTheme.Light" parent="android:Theme.Light">
    </style>
    <style name="WheelDemoTheme" parent="android:Theme">
    </style>
</resources>
